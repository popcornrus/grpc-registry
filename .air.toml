root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/grpc-registry ./cmd/server"
  bin = "./tmp/grpc-registry"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000

[log]
  time = true